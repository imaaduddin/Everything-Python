# function = a block of code which is executed only when it is called.

def hello (name, age):
  print("Hello " + name)
  print("You are " + str(age) + " years old")

first_name = "Naruto"
current_age = 19
hello(first_name, current_age)